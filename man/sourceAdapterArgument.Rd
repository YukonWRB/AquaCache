% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_adapter_registry.R
\name{sourceAdapterArgument}
\alias{sourceAdapterArgument}
\title{Describe one source-adapter argument}
\usage{
sourceAdapterArgument(
  name,
  source = c("user", "runtime", "environment", "internal"),
  help,
  label = NULL,
  value_type = NULL,
  control = NULL,
  required = FALSE,
  default = NULL,
  choices = NULL,
  minimum = NULL,
  maximum = NULL,
  step = NULL,
  advanced = FALSE
)
}
\arguments{
\item{name}{Name of an argument in the registered R function signature.}

\item{source}{One of \code{"user"}, \code{"runtime"}, \code{"environment"}, or
\code{"internal"}.}

\item{help}{Non-blank user-facing explanation of the argument and, for
managed arguments, how it is supplied.}

\item{label}{User-facing control label. Required when \code{source = "user"}.}

\item{value_type}{Storage type for a user argument: \code{"character"},
\code{"numeric"}, \code{"integer"}, \code{"logical"}, \code{"character_vector"}, or
\code{"numeric_vector"}.}

\item{control}{Client control for a user argument: \code{"text"}, \code{"password"},
\code{"numeric"}, \code{"checkbox"}, \code{"select"}, or \code{"multiselect"}.}

\item{required}{Whether a user must supply the argument.}

\item{default}{Optional default value shown by clients.}

\item{choices}{Optional allowed values, required for select controls.}

\item{minimum, maximum, step}{Optional numeric control constraints.}

\item{advanced}{Whether clients should place the control in an advanced
section.}
}
\value{
A validated JSON-compatible argument descriptor.
}
\description{
Creates and validates one argument descriptor for the versioned schema stored
in \code{public.source_adapter_capabilities.argument_schema}. Package developers
can use these descriptors in database patches when registering a new source
adapter or changing the documented arguments of an existing adapter.
}
\details{
Every argument is classified by who supplies it. \code{"user"} arguments are
exposed as controls in clients such as YGwater. \code{"runtime"},
\code{"environment"}, and \code{"internal"} arguments are displayed as read-only
documentation explaining how the adapter receives them.
}
