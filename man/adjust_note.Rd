% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_functions.R
\name{adjust_note}
\alias{adjust_note}
\title{Adjust time-ranged notes for a timeseries}
\usage{
adjust_note(con, timeseries_id, data, delete = FALSE, source_update = FALSE)
}
\arguments{
\item{con}{A connection to the AquaCache database with write privileges on
\code{continuous.notes}.}

\item{timeseries_id}{A single integer timeseries identifier.}

\item{data}{A data frame with \code{note}, \code{start_dt}, and \code{end_dt} columns.
\code{start_datetime}/\code{end_datetime} are accepted aliases. An optional
\code{note_id} updates an existing row, and optional \code{no_source_update} values
control protection from source synchronization.}

\item{delete}{Logical. If \code{TRUE}, remove stored notes beginning at or after
the earliest supplied start datetime when they are not represented in
\code{data}.}

\item{source_update}{Logical. Set \code{TRUE} for source-adapter or
synchronization input. Protected stored rows are then left unchanged, and
incoming rows cannot set \code{no_source_update}.}
}
\value{
Invisibly returns the integer \code{note_id} values of the adjusted rows.
}
\description{
Inserts or updates free-text notes that apply to explicit datetime ranges.
Different notes may overlap. Rows with a \code{note_id} update that note. Rows
without one reuse an exact existing note or are inserted. When
\code{delete = TRUE}, stored notes beginning at or after the earliest supplied
\code{start_dt} are removed unless they are included in \code{data}, matching the
synchronization boundary used by the other \verb{adjust_*()} helpers.
}
