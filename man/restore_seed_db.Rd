% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore_seed_db.R
\name{restore_seed_db}
\alias{restore_seed_db}
\title{Restore an AquaCache seed database from a SQL dump}
\usage{
restore_seed_db(
  file,
  name = Sys.getenv("aquacacheName", "aquacache"),
  host = Sys.getenv("aquacacheHost", "localhost"),
  port = Sys.getenv("aquacachePort", "5432"),
  username = Sys.getenv("aquacacheAdminUser", "postgres"),
  password = Sys.getenv("aquacacheAdminPass", "postgres"),
  maintenance_db = "postgres",
  replace = FALSE,
  require_current_patch = TRUE,
  apply_patches = TRUE,
  psql = NULL,
  cleanup_on_error = TRUE,
  nhn = TRUE
)
}
\arguments{
\item{file}{Path to a plain \code{.sql} dump or gzipped SQL dump.}

\item{name}{Target database name to create and restore into. Defaults to
\code{aquacacheName} from the user's \code{.Renviron}.}

\item{host}{Database host address. Defaults to \code{aquacacheHost}.}

\item{port}{Database port. Defaults to \code{aquacachePort}.}

\item{username}{PostgreSQL user with permission to create and drop the target
database. Defaults to \code{aquacacheAdminUser}.}

\item{password}{Password for \code{username}. Defaults to \code{aquacacheAdminPass}.}

\item{maintenance_db}{Existing database used for the administrative
connection that creates/drops \code{name}. Defaults to \code{"postgres"}.}

\item{replace}{If \code{TRUE}, drop and recreate an existing target database.
\code{postgres}, \code{template0}, and \code{template1} are not allowed.}

\item{require_current_patch}{If \code{TRUE}, the restored database must end at the
latest AquaCache patch available in the installed package.}

\item{apply_patches}{If \code{TRUE}, apply package patches after restore when the
dump is older than the installed package.}

\item{psql}{Path to the \code{psql} utility. If \code{NULL}, the package searches the
system PATH and common PostgreSQL installation directories.}

\item{cleanup_on_error}{If \code{TRUE}, drop the newly created target database if
restore or validation fails.}

\item{nhn}{If \code{TRUE}, offer to download and load the National Hydro Network basins dataset after restore, which is used for location code generation in the Shiny application. Defaults to \code{TRUE}.}
}
\value{
Invisibly returns a list with the restored database name, connection
host/port, and final patch number.
}
\description{
Restores a plain \code{.sql} or gzipped SQL AquaCache seed dump into a new
PostgreSQL database. The target database must be fresh unless \code{replace = TRUE}
is supplied. System databases such as \code{postgres}, \code{template0}, and \code{template1}
are always refused as targets. The restore also ensures the cluster-level
\code{public_reader} login role exists with password \code{"aquacache"} because
AquaCache row-level security policies and grants can reference it during
restore, and that login is useful for read-only applications.
}
\details{
The restore is run through the PostgreSQL \code{psql} command line utility because
dumps created by \code{pg_dump} can contain \code{COPY} data, functions, triggers, and
other SQL that should be interpreted by PostgreSQL's own client.
}
