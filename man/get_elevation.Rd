% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_elevation.R
\name{get_elevation}
\alias{get_elevation}
\title{Get elevation at a geographic coordinate}
\usage{
get_elevation(
  lat,
  lon,
  source = c("arcticdem", "usgs", "cdem", "cdsm"),
  details = TRUE
)
}
\arguments{
\item{lat}{Numeric scalar. Latitude in decimal degrees (WGS84).}

\item{lon}{Numeric scalar. Longitude in decimal degrees (WGS84).}

\item{source}{Character vector giving the elevation sources to try, in
order. Valid values are \code{"usgs"}, \code{"arcticdem"}, \code{"cdem"}, and \code{"cdsm"}.}

\item{details}{Logical. If \code{FALSE}, return only the elevation in metres.
If \code{TRUE}, return a list containing elevation and source metadata.}
}
\value{
If \code{details = FALSE}, a numeric scalar giving elevation in metres, or
\code{NA_real_} if none of the requested sources return a value.

If \code{details = TRUE}, a list containing \code{elevation}, \code{source},
\code{resolution}, and \code{vertical_datum}.
}
\description{
Queries one or more public elevation services and returns the first
available elevation.
}
\details{
The default source order is:
\itemize{
\item \code{"arcticdem"}: ArcticDEM 2 m mosaic from the Polar Geospatial Center. Prefer this when available for highest accuracy north of the US/Canada border, though be aware that it is a DSM and not a DTM.
\item \code{"usgs"}: USGS 3DEP Elevation Point Query Service. United States and Canada, including Alaska. Locations in the US most often benefit from LIDAR and a cell size of 1 meter, 5 meters in parts of Alaska.
\item \code{"cdem"}: Canadian Digital Elevation Model.
\item \code{"cdsm"}: Canadian Digital Surface Model. Not available above 60 degrees of latitude, but in that case you should prefer the ArcticDEM over the cdem; this is just a fall-back.
}

No API keys are required for the default sources.
}
