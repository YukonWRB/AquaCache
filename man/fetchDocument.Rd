% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetchDocument.R
\name{fetchDocument}
\alias{fetchDocument}
\title{Extract document stored as BYTEA from postgreSQL DB}
\usage{
fetchDocument(
  id,
  id_col = "document_id",
  ext = "format",
  table = "documents",
  con = hydrometConnect(silent = TRUE),
  save_dir = NULL,
  save_name = NULL
)
}
\arguments{
\item{id}{The unique ID from the column specified in \code{id_col} with which to identify the individual record containing the binary object.}

\item{id_col}{The column in which to look for the \code{id}}

\item{ext}{The column in which to look for the file extension, or a file extension (must be preceeded by a period). Only used if save_path is not NULL}

\item{table}{The table to look in for the document.}

\item{con}{A connection to the database.}

\item{save_dir}{A directory in which to write the file.}

\item{save_name}{The name to give the file, \emph{without} extension (specify the extension in \code{ext}).}
}
\value{
A data.frame containing the row(s) identified by the specified ID.
}
\description{
Extracts a document/file stored in a postgreSQL database as BYTEA type. Should be flexible enough to work with most schemas, but was designed around the 'documents' table created with this package and with files uploaded to the database with \code{\link[=addHydrometDocument]{addHydrometDocument()}}.
}
