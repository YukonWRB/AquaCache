% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateACDatabaseReference.R
\name{generateACDatabaseReference}
\alias{generateACDatabaseReference}
\title{Generate a human-readable HTML reference for the AquaCache database}
\usage{
generateACDatabaseReference(
  con = NULL,
  output_file = "aquacache_database_reference.html",
  schemas = NULL,
  check_visibility = TRUE,
  open = interactive()
)
}
\arguments{
\item{con}{A DBI connection to the AquaCache database. If \code{NULL}, the
function attempts to create a temporary connection from environment
variables.}

\item{output_file}{File path for the generated HTML document.}

\item{schemas}{Optional character vector of schema names to document. The
default \code{NULL} documents the standard AquaCache schemas.}

\item{check_visibility}{Logical. If \code{TRUE}, compare the visible object counts
of the supplied connection against an admin session when possible and warn
if the connection appears to be missing documented objects.}

\item{open}{Logical. If \code{TRUE}, open the generated HTML file in the default
browser after it is written.}
}
\value{
Invisibly returns the normalized output path.
}
\description{
Generates a large, navigable HTML document describing the AquaCache schema
for analysts, developers, and other readers who need more context than a raw
database dump provides. The output includes schema overviews, tables, views,
functions, keys, foreign-key relationships, triggers, indexes, and column
glossaries. Existing database comments are used whenever available, and
inferred descriptions are clearly marked.

If no connection is supplied, the function creates a temporary connection
with \code{\link[=AquaConnect]{AquaConnect()}} and closes it automatically when generation finishes.
}
