% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbAppendTableRLS.R
\name{dbAppendTableRLS}
\alias{dbAppendTableRLS}
\title{RLS-safe table append}
\usage{
dbAppendTableRLS(
  con,
  target,
  value,
  method = c("auto", "staging", "bind"),
  chunk_size = 10000L,
  on_conflict = c("error", "nothing", "update"),
  conflict_cols = NULL,
  update_cols = NULL
)
}
\arguments{
\item{con}{A DBI connection.}

\item{target}{A table identifier accepted by DBI (character, \code{\link[DBI:Id]{DBI::Id()}}, or \code{\link[DBI:SQL]{DBI::SQL()}}).}

\item{value}{A data.frame or data.table containing rows to append.}

\item{method}{One of \code{"auto"}, \code{"staging"}, or \code{"bind"}.}

\item{chunk_size}{Number of rows per batch for \code{method = "bind"} (not used for \code{method = "staging"}).}

\item{on_conflict}{Conflict behavior: one of \code{"error"}, \code{"nothing"}, or \code{"update"}.}

\item{conflict_cols}{Character vector of conflict key columns used for \code{on_conflict = "nothing"} or \code{"update"}.}

\item{update_cols}{Character vector of columns to update for \code{on_conflict = "update"}.}
}
\value{
Integer number of rows inserted/affected.
}
\description{
Appends rows to a database table while remaining compatible with PostgreSQL Row-Level Security (RLS). Avoids direct \verb{COPY FROM} statements into RLS-protected tables, which is not allowed by postgres unless the user is specified as BYPASSRLS or is a superuser. The trade off is that this function is slower than \code{DBI::dbAppendTable()} for large datasets, but it will work in more scenarios and is more robust to schema changes. The default method is "staging" for PostgreSQL connections, which uses a temporary staging table to perform the insert, and "bind" for other databases, which performs batched inserts using parameterized queries. The \code{on_conflict} argument allows you to specify how to handle conflicts with existing rows based on specified key columns.
}
\examples{
\dontrun{
con <- AquaConnect()
dbAppendTableRLS(con, "organizations", data.frame(name = "Example Org"))
DBI::dbDisconnect(con)
}
}
